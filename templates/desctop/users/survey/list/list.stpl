<% include!("../../../../base_block/base_header.stpl"); %>

<span class="load_block">
  <div data-title="<%= user.get_full_name() %> - список опросов <%= list.name %>" class="main-container mt-3 pk_saver" data-pk="<%= list.id %>" data-type="user_survey_list">
    <span class="load_block" data-link="/survey/load_list/<%= list.id %>/">

    </span>
  </div>

  <% include!("../../../../base_block/base_footer.stpl"); %>
</span>
